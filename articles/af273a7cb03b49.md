---
title: "nixpkgsをoverrideして古いパッケージを新しくしよう！"
emoji: "😽"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["NixOS", "Nix", "Linux"]
published: false
---

## 背景
Nixでパッケージを管理していると、[nixpkgs](https://github.com/NixOS/nixpkgs)のマージが遅くて最新のパッケージが使えないことがあります。特に、最新版を常に必要とするパッケージでは致命的です。

## Let's override!
ほとんどの場合は、[nixpkgsのPR](https://github.com/NixOS/nixpkgs/pulls)を探すと最新のパッチがあります。

### シングルファイルで構成されたパッケージの場合
```nix
nixpkgs.overlays = [
  (final: prev: {
    パッケージ名(e.g, google-chrome) = final.callPackage (builtins.fetchurl {
      url = "https://raw.githubusercontent.com/vikingnope/nixpkgs/7d24905cd37a24ad9e811e068c0ca0e9ea23edbb/pkgs/by-name/wi/wivrn/package.nix";
      sha256 = "a71304059ad513a89d2b0c18deb24fcaf9c5ef17bb4dff55f475b98f0d5330ec";
    }) { };
    ...
  })
];
```
